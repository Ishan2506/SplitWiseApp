<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Scanning receipts and group invite QR codes. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Talking to the API. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- The scanners need a camera but the app still works without one:
         receipts can be uploaded from the gallery and invites pasted as codes. -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <application
        android:label="PaisaSplit"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- paisasplit://join/<CODE> — invite links shared from the app.
                 The scheme must stay unique to this app: the generic
                 "splitwise" is claimed by the Splitwise app, so a link using
                 it can open that app instead of this one. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="paisasplit" android:host="join"/>
            </intent-filter>

            <!-- http://<dev-server>/join/<CODE> — the link behind every group
                 QR code while developing. It must match INVITE_BASE_URL in the
                 server's .env; update both together if the LAN IP changes.

                 Note there is no android:autoVerify here: Android only verifies
                 https hosts that serve /.well-known/assetlinks.json, so this
                 opens a chooser rather than jumping straight into the app. Once
                 you have a real domain, switch the scheme to https, point the
                 host at it, serve assetlinks.json, and add autoVerify="true". -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="http"
                      android:host="103.212.121.139"
                      android:port="7000"
                      android:pathPrefix="/join"/>
            </intent-filter>
        </activity>
        <!-- Have Play Services fetch the OCR model when the app is installed,
             so the first receipt scan does not stall on a download. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="ocr" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
